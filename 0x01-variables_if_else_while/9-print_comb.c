#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int max;

for (max = 0; max < 10; max++)
{
putchar(max + '0');
if (x < 9)
{
putchar(',');
putchar(' ');
}
}
putchar('\n')

return (0);
}
